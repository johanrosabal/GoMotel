rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Abrir temporalmente para configuración inicial de producción
      allow read, write: if true;
    }
  }
}
